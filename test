local function decode(t)
    local s = ""
    for i = 1, #t do
        s ..= string.char(t[i])
    end
    return s
end

local whitelist = {}
whitelist[decode({50,54,48,73,68})] = true
whitelist[decode({65,100,109,105,110,49,50,51})] = true

return whitelist
